"""Whole-user insight generation: aggregates a user's scored transactions,
subscriptions, and account balances into `insights` rows. See generator.py
for the main entry point, generate_insights(user_id).
"""
